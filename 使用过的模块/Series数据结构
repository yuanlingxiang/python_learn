1、Series：有一组数据以及对应的索引组成

创建Series
obj = Series(seq)  索引默认从0开始
obj = Series(seq, index=list)  指定Series的索引

获取Series的值
obj.values

获取Series的索引
obj.index

修改Series的索引
obj.index = list1
___________________________________________________________
2、索引

基本索引
obj[ind]：获取索引为ind的值
obj[[ind1, ind2, ind6]]:通过索引获取几个元素

bool索引
obj[obj>0]：获取obj大于0的元素，obj>0返回一个bool数组

___________________________________________________________
3、关系运算符

判断某个值是否在Series中,返回bool值
v1 in obj
v1 not in obj
___________________________________________________________
4、转换

将字典转换为Series结构
obj = Series(dict1)

___________________________________________________________
5、检查
Series(s1, index)：筛选出索引在为index的值
s1：为Series
index：为一个索引列表

___________________________________________________________
6、空值检查
pd.isnull(obj):返回bool数组
pd.notnull(obj):返回bool数组
___________________________________________________________
7、算数运算
obj1 + obj2 :索引自动对齐，只在一个Series中存在的值计算结果为NaN

___________________________________________________________









