1、集合：
集合对象是一组无序排列的可哈希的值，因为无序特性，不能通过索引和切片访问其中的元素

通常用来取两个集合的交集，并集，差集，对称差

分类：
可变集合和不可变集合

复合运算：
运算的结果与操作符左边的类型保持一致，即如果左侧是可变集合，结果类型也是可变集合

集合的操作符和方法不同点：
操作符：两边需为集合类型
方法：操作对象可以是迭代类型的
_____________________________________________________________________________________
2、创建集合
创建可变集合：
s = set(seq)

创建不可变集合：
s = frozenset(seq)
_____________________________________________________________________________________
3、访问集合元素
通过for循环迭代
_____________________________________________________________________________________
4、集合内建方法
求并集的方法
s1.update(t2)

交集方法：
s1.intersection_update(t1)

差集的方法：
s1.difference_update(t1)

对称差方法：
s1.symmetric_difference_update(t1)

_____________________________________________________________________________________
添加元素
obj：需是可hash的

添加元素到集合中：
s1.add(obj)
_____________________________________________________________________________________
删除元素
从集合中删除元素， obj1不存在抛出异常：
s1.remove(obj1)

从集合中删除元素， obj1不存在不抛出异常：
s1.discard(obj1)

从集合中删除元素，并返回删除的元素
s1.pop()

移除集合中的所有元素
s1.clear()
_____________________________________________________________________________________
判断
判断s1是否是t1的子集，返回bool值：
s1.issubset(t1)

判断s1是否是t1的超集，返回bool值：
s1.issuperset(t1)


